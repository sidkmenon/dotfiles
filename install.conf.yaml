- defaults:
    link:
        create: true
        relink: true
- clean: ['~']
- link:
    ~/.doom.d:
    ~/.vim:
    ~/.gitconfig:
    ~/.zshrc:
- create:
    - ~/workspace
- shell:
    - [git submodule update --init --recursive, Installing submodules]
